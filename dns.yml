# tady bude ansible code pro dns bro
https://github.com/PowerDNS/pdns-ansible

# Tady je vše co k tomu je potřeba + napojení na MySQL / SQLLite
https://github.com/PowerDNS/pdns-ansible/tree/master/tasks

---
- hosts: all
  sudo: true
  become: yes
  become_user: root
  roles:
    - { role: PowerDNS.pdns }
  vars:
    pdns_config:
      master: true
      slave: false
      local-address: '192.0.2.77'
    pdns_backends:
      gmysql:
        host: 192.0.2.120
        port: 3306
        user: powerdns
        password: P0w3rDn5
        dbname: pdns
    pdns_mysql_databases_credentials:
      gmysql:
        priv_user: root
        priv_password: myrootpass
        priv_host:
          - "%"
    pdns_install_repo: "{{ pdns_auth_powerdns_repo_41 }}"


https://cloud.garr.it/support/kb/general/installAuthoritativeDNS/
